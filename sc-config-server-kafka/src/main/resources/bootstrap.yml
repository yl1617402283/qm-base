server:
  port: 7861
spring:
  application:
    name: sc-config-server-kafka
  cloud:
    config:
      server:
        git:
          uri: https://github.com/lupeng-22/repo.git
          search-paths: local
          username: 18235960226@163.com
          password: 19900522lupeng
          force-pull: true
          basedir: /data/config-repo
eureka:
  instance:
    prefer-ip-address: true
  client:
    serviceUrl:
      defaultZone: http://localhost:8669/eureka/

management:
  security:
    enabled: false
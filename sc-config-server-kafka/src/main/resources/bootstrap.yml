server:
  port: 7861
spring:
  application:
    name: sc-config-server-kafka
  cloud:
    config:
      server:
        git:
          uri: http://192.168.1.124/repo/repo.git
          search-paths: local
          username: lupeng
          password: 199022lupeng
          force-pull: true
          basedir: /data/config-repo
eureka:
  instance:
    prefer-ip-address: true
  client:
    serviceUrl:
      defaultZone: http://192.168.1.176:8669/eureka/

management:
  security:
    enabled: false